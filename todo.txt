================================================
TO-DO 
================================================

PRIORITAS TINGGI (SELESAI):
[x] 1. Perbaiki menu Wizard (3 kelompok fitur + separator)
[x] 2. Test pada proyek React/JSX (Sudah fix dan solid)
[x] 3. Refactor dependencyAnalyzer.js → modul resmi terintegrasi
[x] 4. Peningkatan Safety & Confidence (Bailout warning, label status, auto-fix safe only)

[x] 5. Config file .deadkillerrc.json
        - SELESAI: Ditambahkan opsi `mode` (vanilla/react/next) ke ruleEngine.js
        - SELESAI: Ditambahkan `ignoreDependencies` untuk ignore manual
        - SELESAI: Framework-aware auto-protection (pages/, app/, api/ untuk Next.js)
        - SELESAI: File contoh .deadkillerrc.example.json dibuat
[x] 6. Output JSON mode
        - SELESAI: Ditambahkan flag --json ke perintah scan
        - SELESAI: Output JSON terstruktur dengan confidence/status per temuan
        - SELESAI: Cocok untuk integrasi CI/CD pipeline
[x] 7. Perbaiki tampilan diff di terminal
        - SELESAI: Git-style format dengan box-drawing characters (┌ │ └)
        - SELESAI: Statistik +additions -deletions di footer
        - SELESAI: Pewarnaan lebih baik (header, hunk, context)
[x] 8. Implementasi Sistem Caching Sederhana (Audit Performance)
        - SELESAI: Dibuat src/parser/parseCache.js (in-memory mtime-based cache)
        - SELESAI: Diintegrasikan ke scanCommand.js dan fixCommand.js
        - SELESAI: Menampilkan statistik cache (hits, misses, hit rate)

[x] 9. Reverse trace: "siapa yang mengimport file X?"
        - SELESAI: Perintah baru `deadkiller trace <file>`
        - SELESAI: Menampilkan tree importers (├── └──) dengan nama import
        - SELESAI: Mendeteksi apakah file = entry point atau dead file
        - SELESAI: Juga menampilkan apa yang diimport OLEH file target
[x] 10. Watch mode: auto-scan saat file berubah
        - SELESAI: Perintah baru `deadkiller watch <dir>`
        - SELESAI: Menggunakan `fs.watch` bawaan Node.js (zero external dependency)
        - SELESAI: Debounce 500ms untuk menghindari spam analisis
        - SELESAI: Menampilkan ringkasan safe/risky per file yang berubah
[x] 11. Laporan HTML ringkasan dari hasil scan
        - SELESAI: DIGABUNG ke graphVisualizer.js (bukan file HTML terpisah)
        - SELESAI: Section Dead Code Report di bawah graf (Safe/Review/Risky + Dead Files)
        - SELESAI: Style konsisten dengan dashboard (card, badge, table)
        - SELESAI: Dark mode toggle (🌓) + bilingual (ID/EN) untuk report section
        - SELESAI: `deadkiller report` = alias dari `deadkiller visualize`

[x] 12. Automated Test Suite (Unit Test)
        - SELESAI: 55 test cases, 12 test suites, 100% PASS
        - SELESAI: Node.js built-in test runner (`node:test` + `node:assert`)
        - SELESAI: Dead Code Analyzer (33 tests):
          * Unused variable, let, var, multiple deklarasi
          * Unused function, arrow, expression, rekursi
          * Unused import (named, default, alias, namespace)
          * Unreachable code (return, throw, fallthrough)
          * Write-only variable, compound assignment
          * Confidence scoring (high/medium/low) & status (safe/review/risky)
          * Destructuring (object & array)
          * Block scope & inner scope isolation
        - SELESAI: Code Cleaner (8 tests):
          * Penghapusan baris tunggal & multiple dead nodes
          * Proteksi DuplicateCondition, Parameter, ClassMethod
          * Null/empty deadNodes handling
        - SELESAI: Rule Engine (7 tests):
          * Variabel berawalan _ di-skip
          * Framework mode (next/vanilla)
          * ignoreDependencies filter
        - SELESAI: Scope System (5 tests):
          * Read/write tracking, parent chain, self-reference
        - SELESAI: AST Parser (2 tests):
          * Valid parse & ParseError handling
        - Waktu eksekusi: ~420ms

[x] 13. Perbarui README.md Sesuai Kondisi Terkini
        - SELESAI: Struktur direktori lengkap (5 grup: parser, analyzer, eliminator, commands, ui)
        - SELESAI: Dokumentasi SEMUA 8 commands + opsi --json + contoh output
        - SELESAI: Section konfigurasi .deadkillerrc.json (tabel opsi lengkap)
        - SELESAI: Mermaid internal dependency graph (semua 30+ modul terhubung)
        - SELESAI: Flowchart 4-phase pipeline (Graph → Analysis → Classification → Output)
        - SELESAI: Section Confidence & Safety system
        - SELESAI: Tabel dependensi utama

[x] 14. Perkuat Error Handling & Edge Cases
        - SELESAI: Parse error di projectGraph.js sekarang beri WARNING (bukan silent skip)
        - SELESAI: Parse error di scanCommand.js & fixCommand.js beri warning
        - SELESAI: Filter ekstensi file diganti whitelist Set (bukan blacklist manual)
          → Hanya .js .jsx .mjs .cjs .ts .tsx .mts yang di-parse
        - SELESAI: JSON mode tidak lagi menghapus terminal (hapus \x1Bc)
        - SELESAI: fixCommand globalRegistry bug diperbaiki (new Map → null)
        - SELESAI: Constant folding diperkuat: if(0), if(null), if(""), if(undefined), void 0
        - SELESAI: 5 test baru (TC-56 s/d TC-60), total 60 tests 100% PASS

[x] 22. Kurangi False Positive pada Side-Effect Imports
        - SELESAI: import './polyfill.js' dan import 'reflect-metadata' di-skip
        - SELESAI: Side-effect import (tanpa specifier) tidak masuk scope
        - SELESAI: Build tools auto-ignore (@types/*, eslint*, typescript, webpack, dll)
        - SELESAI: 3 test (TC-86, TC-87, TC-88) PASS

[ ] 23. Perkuat Deteksi Callback & Higher-Order Function
        Masalah: Fungsi yang dipakai sebagai callback salah deteksi.
        Contoh: `arr.map(transform)` → transform DIPAKAI
        Perbaikan:
        - Identifier sebagai argumen CallExpression → READ reference
        - Test case untuk addEventListener, .map, .filter, middleware

[ ] 24. Perkuat JSX/TSX Awareness
        Masalah: Komponen React di JSX bisa salah deteksi.
        Contoh: `<Header />` → Header DIPAKAI
        Perbaikan:
        - Test mendalam: spread props, conditional render, Fragment
        - Dynamic component: `const C = isAdmin ? Admin : User`

[x] 25. Deteksi Redundant Code (Kategori 3) — SELESAI PENUH
        - SELESAI: Duplicate Import detection (TC-89, TC-90, TC-91)
        - SELESAI: Redundant Assignment x=1; x=2; (TC-100, TC-101)
        - SELESAI: Self-Assignment x=x; (TC-102)
        - SELESAI: Redundant Return / return undefined (TC-103, TC-104, TC-105)
        - SELESAI: Useless Expression standalone literal (TC-106)
        - File baru: redundancyAnalyzer.js

[x] 26. Dead Code di Switch-Case
        - SELESAI: Duplicate switch case (TC-107)
        - SELESAI: Duplicate if condition chain (TC-108)
        - SELESAI: switch(true) default unreachable (TC-109, TC-110)
        - Kode setelah break sudah ditangani oleh branchAnalyzer

[x] 27. Deteksi Empty Functions & Empty Blocks
        - SELESAI: Empty function body (FunctionDeclaration, FunctionExpression, Arrow)
        - SELESAI: Empty catch block (error silently swallowed)
        - SELESAI: Label confidence MEDIUM, status REVIEW
        - SELESAI: 3 test (TC-80, TC-81, TC-82) PASS


[ ] 28. TypeScript-Specific Dead Code
        - `import type { X }` → unused jika tidak dipakai
        - Unused enum members
        - Unused interface properties
        - Type assertion tidak perlu

[ ] 29. Unused Exported Functions (Cross-File Deep)
        - Mode `preserveExports: 'strict'`
        - Export yang tidak pernah di-import → REVIEW
        - Hati-hati: library export untuk consumer external

[x] 30. Analisis Short-Circuit & Ternary Dead Code
        - SELESAI: false && doSomething() → right side dead
        - SELESAI: true || fallback() → right side dead
        - SELESAI: false ? dead : alive → consequent dead
        - SELESAI: true ? alive : dead → alternate dead
        - SELESAI: 4 test (TC-72, TC-73, TC-74, TC-75) PASS

[x] 31. Deteksi Unreachable Code dalam Loop
        - SELESAI: while(false) { dead(); } → body dead
        - SELESAI: for(;false;) { dead(); } → body dead
        - SELESAI: while(0) → body dead
        - SELESAI: Loop always-break: for(...){ break; } → useless loop
        - SELESAI: 6 test (TC-69 s/d TC-71, TC-83 s/d TC-85) PASS

[x] 32. Negasi Operator di Constant Folding
        - SELESAI: if(!true), if(!false), if(!0), if(!1) → evaluasi negasi
        - SELESAI: if(!!false) → double negasi
        - SELESAI: 5 test (TC-61 s/d TC-65) PASS

[x] 33. Constant Propagation (Basic)
        - SELESAI: Kumpulkan const dengan nilai literal di top-level scope
        - SELESAI: const FLAG = false; if(FLAG) → dead branch
        - SELESAI: const DEBUG = true; if(DEBUG) → else dead
        - SELESAI: Propagation ke while/for/ternary/short-circuit
        - SELESAI: 6 test (TC-76 s/d TC-79, TC-92, TC-93) PASS

[x] 34. Redundant Boolean Logic
        - SELESAI: if(flag && false) → always false
        - SELESAI: if(true || x) → always true
        - SELESAI: if(false || false) → always false
        - SELESAI: evaluateStaticBool() recursive pada LogicalExpression
        - SELESAI: 3 test (TC-66, TC-67, TC-68) PASS

[x] 35. CSS/Non-JS File Parse Skip (projectGraph.js)
        - SELESAI: BFS graph sekarang skip non-JS files (.css, .json, .svg, dll)
        - SELESAI: File tetap masuk liveFiles tapi tidak di-parse
        - SELESAI: Whitelist Set: .js .jsx .mjs .cjs .ts .tsx .mts

[x] 36. Analisis Semantik & Data Flow (Status Per Sub-Fitur)
        - SELESAI: Semantic Analysis — scope tracking, read/write, variable lifecycle
        - SELESAI: Constant Folding & Propagation — evaluateStaticBool() + collectConstantMap()
        - SELESAI: Boolean Logic Simplification — &&, ||, !, !! recursive evaluation
        - SELESAI: Improved Unreachable Detection — after return/throw/break/continue
        - SELESAI: Dead Branch Detection — if(false), if(FLAG), if(!true), if(0)
        - SELESAI: Loop Analysis — while(false), for(;false;), loop always-break
        - SELESAI: Condition Contradiction — x&&!x, x==='a'&&x==='b', x>10&&x<5 (TC-94~99)
        - SELESAI: Redundant Code — assignment, self-assign, return, expression (TC-100~106)
        - SELESAI: Data Flow Analysis — redundant assignment tracking inter-block (redundancyAnalyzer.js)
        - SELESAI: Function Call Graph — caller→callee mapping + orphan detection (TC-115~117)
        - SELESAI: Control Flow Graph (CFG) — basic blocks, edges, unreachable detection (TC-111~114)
        - SELESAI: Path-sensitive Analysis — typeof guard tracking, undefined-in-else (TC-118~120)

[x] 37. Perkuat Deteksi Callback & Higher-Order Function
        - SELESAI: arr.map(transform) → transform terdeteksi READ
        - SELESAI: addEventListener, .filter, middleware pattern valid
        - SELESAI: 3 Test Cases (TC-121, TC-122, TC-123) 100% PASS

[x] 38. Perkuat JSX/TSX Awareness
        - SELESAI: <Header /> → Header DIPAKAI (Valid)
        - SELESAI: Dynamic component: const C = isAdmin ? Admin : User (Valid)
        - SELESAI: Spread props valid, Fragment valid
        - SELESAI: 3 Test Cases (TC-124, TC-125, TC-126) 100% PASS

[x] 39. TypeScript-Specific Dead Code
        - SELESAI: import type { X } → unused jika tidak dipakai (UnusedType)
        - SELESAI: Unused enum, interface, type alias masuk UnusedType
        - SELESAI: Klasifikasi terpisah dari 'Variable' (confidence: HIGH, status: SAFE)
        - SELESAI: 3 Test Cases (TC-127, TC-128, TC-129) 100% PASS

[x] 40. Unused Exported Functions (Cross-File Deep)
        - SELESAI: Mode `preserveExports: 'strict'` ditambahkan ke exportAnalyzer.js
        - SELESAI: Export yang tidak pernah di-import oleh file manapun dalam project ditandai 'review'
        - SELESAI: Tetap memproteksi export di mode library (`preserveExports: true`)
        - SELESAI: 1 Test Case (TC-130) PASS
Tidak ada node JSXElement yang dideteksi di kode manapun di src/
Tidak ada modul reactAnalyzer.js


[ ] 41. Summary Statistics Box di Akhir Scan
[ ] 42. Perintah `init` — Generate .deadkillerrc.json Interaktif
[ ] 43. Perbaiki Output `--json` agar Lengkap
[ ] 44. Performance Benchmark & Optimization
Memperbaiki Bug: Memperbaiki 1 kegagalan deteksi (False Negative) di namespace Utility (TC-08)?



================================================
ITEM YANG SUDAH DIKERJAKAN (DIHAPUS DARI BACKLOG)
================================================
Berikut item dari daftar lama yang SUDAH ada di codebase:

✅ "Pisahkan klasifikasi Dead Code vs Unused Code"
   → Sudah: classifyConfidence() membedakan Variable, Function, DeadCode,
     DeadBranch, WriteOnly, Parameter, ClassMethod, RedundantCode, EmptyBlock

✅ "Perbaiki rule unused function: jika tidak dipanggil → HIGH"
   → Sudah: Function → confidence MEDIUM, status REVIEW (aman karena
     bisa callback). Jika benar-benar orphan, flowAnalyzer.js deteksi.

✅ "Perbaiki deteksi entry point (Next.js): route.ts, page.tsx, layout.tsx"
   → Sudah: entryPointFinder.js line 63 → 'app/**/*.{js,jsx,ts,tsx}'
     mencakup route.ts, page.tsx, layout.tsx, middleware.ts otomatis.

✅ "Tambahkan analisis read vs write (data-flow sederhana)"
   → Sudah: scope.js readCount/writeCount, WriteOnly detection.

✅ "Perbaiki false positive pada file yang dianggap tidak terhubung"
   → Sudah: ruleEngine.js frameworkPreservedPaths proteksi pages/, app/, api/

✅ "Implementasi constant folding sederhana (const FLAG = false)"
   → Sudah: branchAnalyzer.js collectConstantMap() + evaluateStaticBool()

✅ "Deteksi kondisi boolean statis: true && false, A && !A"
   → Sudah: branchAnalyzer.js evaluateStaticBool() + logicAnalyzer.js hasContradiction()

✅ "Tambahkan basic logical contradiction detection"
   → Sudah: logicAnalyzer.js — x&&!x, x==='a'&&x==='b', x>10&&x<5

✅ "Tingkatkan control flow analysis (CFG sederhana)"
   → Sudah: flowAnalyzer.js buildCFG() — basic blocks, edges, unreachable

✅ "Turunkan severity untuk class property/method yang unused"
   → Sudah: ClassMethod → confidence LOW, status RISKY

✅ "Perbaiki deteksi unused parameter (hindari false positive)"
   → Sudah: Parameter → confidence LOW, status RISKY (tidak auto-delete)

✅ "Tambahkan confidence score pada setiap hasil deteksi"
   → Sudah: Setiap temuan memiliki confidence (high/medium/low) + status

✅ "Tambahkan safe auto-fix (preview + backup sebelum delete)"
   → Sudah: checkpoint system (backup/restore) + diff preview + proteksi type

================================================
ITEM FUTURE WORK (Tidak dikerjakan sekarang)
================================================
Fitur berikut memerlukan riset/dependency eksternal yang signifikan:

- TypeScript Compiler API (ts.createProgram) → butuh dependency berat
- Inter-file data flow analysis → symbolic execution, sangat kompleks
- Dynamic usage: obj["method"], eval() → fundamental limitation static analysis
- Full framework awareness (Next.js server actions, etc) → terus berubah
- Precision/Recall/F1-score evaluation → butuh ground truth dataset
